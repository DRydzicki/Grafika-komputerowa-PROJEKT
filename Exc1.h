#include"Engine/EngineLoader.h"

namespace Exc1{ 
    const double PI = 3.1415;

    static bool ex1, ex2, ex3, ex4, ex5;
    static bool isNSelected, isRSelected, isZSelected;
    static std::string tempN, tempR, tempZ;

    void DrawWindow();
    void WindowContent(sf::RenderWindow& window);
}
